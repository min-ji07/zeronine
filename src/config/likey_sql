#LIKEY테이블 생성
CREATE TABLE LIKEY(
	LIKEYNO		NUMBER 			PRIMARY KEY,
	BNO 		NUMBER,
	USERID 		VARCHAR2(20),
	LIKEY_YN 	VARCHAR2(20),
	LIKECOUNT 	NUMBER
);

#LIKEY용 시퀀스 생성
CREATE SEQUENCE LIKEY_SEQ 
START WITH 1 
INCREMENT BY 1;
COMMIT; 